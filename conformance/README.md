# Independent conformance skeleton

This unpublished Phase 1 skeleton consumes only the packed normative artifact
manifest and checked-in language-neutral vectors. It does not import `lib/`,
generated runtime codecs, a reference provider, or Foundation code. It is not a
conformance claim or an independent-oracle qualification.

The future black-box runner belongs here. Its expected results must be authored
independently from production implementation output.
