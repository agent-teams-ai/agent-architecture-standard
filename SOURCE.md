# Source

The documents under `docs/research/` are an unchanged historical research
input from
`agent-teams-ai/engineering-foundation`, branch
`docs/agent-architecture-standard-study`, at commit
`75cc49466bb7eebef375b24a870f2a9feeb688cb`.

This repository also contains the private Phase 1 implementation derived from
that input. It is standalone and does not depend on Engineering Foundation.
The immutable `spec/` Status headers describe the normative edition, while this
file describes the repository implementation phase; those statuses are not an
instruction to cascade or revise the versioned `@1` profile identities.
Identifiers and public claims remain provisional and unpublished; D1 neutral
authority relocation remains unresolved as recorded in ADR-0002.
