# Phase 0 accepted decision-record index

Status: accepted for this unpublished Phase 0 normative scaffold; public
governance roles remain unassigned

The current versioned [D0–D11 packet](phase-0-d0-d11-v2.md) records the
commissioning decisions that this scaffold implements. Acceptance here authorizes bounded
specification work only. It does not activate identifiers, qualify conformance,
assign neutral governance roles, authorize publication, or represent a vote by
the research reviewers.

[Version 1](phase-0-d0-d11-v1.md) is preserved as a superseded historical
packet and MUST NOT be used as the current decision authority.

The first commit that checks in this index and version 1 establishes the first
immutable repository preservation anchor for those exact v1 bytes. The anchored SHA-256 is
`ac3bfcc019a80483978f45ec4badc384e38124d601be606ae39566963ba422a6`.
There is no claim that an older Git commit contains version 1. Once this anchor
is checked in, any byte change to version 1 is invalid; verification MUST hash
the file bytes directly and compare the exact lowercase digest above.

Any changed outcome requires a new packet version with explicit compatibility
and migration effects. Historical packets remain immutable after publication.
